next(EA,EA+Size):-
    instruction(EA,Size,_,_,_,_).
    
return(EA):-
    instruction(EA,_,'RET',_,_,_).
    
may_fallthrough(From,To):-
    next(From,To),
    !return(From),
    !inconditional_jump(From).
